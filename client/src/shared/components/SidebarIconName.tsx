import React from 'react'

function SidebarIconName({ name }: any) {
  return (
    <span
      style={{
        textDecoration: "none",
        color: "black",
      }}
    >
      {name}
    </span>
  );
}

export default SidebarIconName
